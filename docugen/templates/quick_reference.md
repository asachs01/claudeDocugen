# {{ title }} - Quick Reference

{% if prerequisites %}
**Requirements:** {{ prerequisites | join(', ') }}
{% endif %}

## Steps

{% for step in steps %}
{{ step.number }}. **{{ step.title }}**{% if step.shortcut %} (`{{ step.shortcut }}`){% endif %}

{% endfor %}

---

*Quick reference generated by DocuGen*
